MacroFire 2.0.1


設定ファイルの書き方
-------------------------------------------------
  設定ファイルは1行が1つの設定に対応しており、
  それぞれの行は以下のフォーマットで書き込まれます。

    設定名 = 設定値

  そしてこれらの設定はさらにセクションごとに別々に管理されます。
  "["と"]"と囲まれた行がセクションの開始を示します。

  これはWindowsのINIファイルと同じフォーマットです。


よくある設定値
-------------------------------------------------
  設定値の内容は、設定によって様々ですが、
  単なる数値やON/OFF以外によく使っているものとして、ボタン定義があります。
  ボタン定義は、ボタンの組み合わせを設定する場合にしようされます。

  それぞれのボタンは以下の表の名前と対応しており、
  これらの名前をひとつ、あるいは「+」でつないで複数列挙します。

      ------------------------
      ボタン名| ボタン
      ------------------------
      SELECT  | セレクトボタン
      START   | スタートボタン
      UP      | 十字キー上
      RIGHT   | 十字キー右
      DOWN    | 十字キー下
      LEFT    | 十字キー左
      LTRIGGER| Lボタン
      RTRIGGER| Rボタン
      TRIANGLE| △ボタン
      CIRCLE  | ○ボタン
      CROSS   | ×ボタン
      SQUARE  | □ボタン
      NOTE    | ♪ボタン
      SCREEN  | 輝度変更ボタン
      VOLUP   | 音量+ボタン
      VOLDOWN | 音量-ボタン
      ------------------------

  たとえば、スタートボタンとセレクトボタンの同時押しにする場合は

      START + SELECT

  のように記述します。
  また、単にボタン一つだけ、たとえば♪ボタンだけを設定する場合には、

      NOTE

  のように、単純に名前だけを記述します。

  ちなみに、いないとは思いますが、

      LEFT + RIGHT
      UP + DOWN
      UP + RIGHT + DOWN + LEFT

  にような設定も可能です。
  ですが、普通PSPの十字ボタンは押した方向と逆側のボタンを同時に押すことはできませんので、
  これは意味をなしません。


-------------------------------------------------
pen@ClassG
http://classg.sytes.net
